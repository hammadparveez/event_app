import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xff5669FF);
  static const lightPrimaryColor = Color(0xFF7D8BF8);
  static const white = Colors.white;
}

class Styles {
  static const smallHeading = TextStyle(fontSize: 22, color: AppColors.white);
}
